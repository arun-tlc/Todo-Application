<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.TodoApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.TodoApp" parent="Base.Theme.TodoApp" />
    
    <style name="WhiteBoxEditTextStyle" parent="Widget.AppCompat.EditText">
        <item name="android:background">@drawable/edit_text_background</item>
        <item name="android:textColor">@android:color/black</item>
        <item name="android:textColorHint">@android:color/darker_gray</item>
    </style>
    
    <style name="WhiteBoxButtonStyle" parent="Widget.AppCompat.Button.Borderless">
        <item name="android:background">@drawable/white_box_background</item>
        <item name="android:textColor">@android:color/black</item>
    </style>
    
    <style name="BlueBoxButtonStyle" parent="Widget.AppCompat.Button.Borderless">
        <item name="android:background">@android:color/holo_blue_light</item>
        <item name="android:textColor">@android:color/white</item>
    </style>
    
    <style name="AppTheme" parent="Base.Theme.TodoApp">
        <item name="android:fontFamily">@font/font</item>
    </style>

    <style name="ArialBlack" parent="Base.Theme.TodoApp">
        <item name="android:fontFamily">@font/arial_black</item>
    </style>
    
    <style name="CikalBakal" parent="Base.Theme.TodoApp">
        <item name="android:fontFamily">@font/cikal_bakal</item>
    </style>

    <style name="TimesNewRoman" parent="Base.Theme.TodoApp">
        <item name="android:fontFamily">@font/times_new</item>
    </style>
</resources>